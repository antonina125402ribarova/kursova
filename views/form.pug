doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Форма за рецепта
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet", href="style.css")
  body
    .container
      h2.mt-5 Добави нова рецепта
      form(action="/recipe", method="POST")
        .mb-3
          label(for="dishName", class="form-label") Име на ястието:
          input.form-control(type="text", id="dishName", name="dishName", required)
        .mb-3
          label(for="image") Снимка:
          br
          input.form-control(type="file", id="image", name="imageMethod", accept="image/*", required)
        .mb-3
          label(for="ingredients", class="form-label") Съставки:
          textarea.form-control(id="ingredients", name="ingredients", rows="4", required)
        .mb-3
          label(for="instructions", class="form-label") Начин на приготвяне:
          textarea.form-control(id="instructions", name="instructions", rows="4", required)
        button.btn.btn-primary(type="submit") Добави рецепта
